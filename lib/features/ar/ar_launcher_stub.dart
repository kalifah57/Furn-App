/// بديل غير-ويب (VM/اختبارات): لا شيء. الواقع المعزّز يعمل في متصفّح الهاتف فقط.
void openArView({
  required String glbUrl,
  required String usdzUrl,
  required String title,
  double? widthCm,
  double? depthCm,
  double? heightCm,
  String? color,
}) {
  // No-op off the web. Kept identical in signature to the web implementation.
}
